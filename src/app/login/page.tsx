export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center px-6">
      <div className="ui-panel w-full max-w-md text-center">
        <h1 className="ui-h1">ORIGO</h1>
        <p className="mt-2 ui-body-muted">
          Inicio de sesión. Placeholder: conectar con Vento OS (Shell) cuando auth esté configurado.
        </p>
        <p className="mt-4 ui-caption">
          Mientras tanto, usa el Panel y las rutas de la app para revisar la estructura.
        </p>
      </div>
    </div>
  );
}
